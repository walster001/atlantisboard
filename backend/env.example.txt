# AtlantisBoard Backend Environment Variables
# Copy this file to .env and fill in your values

# ============================================
# Database Configuration
# ============================================
# PostgreSQL connection string
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/atlantisboard

# ============================================
# JWT Authentication
# ============================================
# Generate strong random secrets (minimum 32 characters)
# For production, use: openssl rand -hex 32
JWT_SECRET=your-jwt-secret-minimum-32-characters-long
JWT_REFRESH_SECRET=your-refresh-token-secret-minimum-32-characters-long

# Token expiration times
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# ============================================
# Server Configuration
# ============================================
# API server port (REST endpoints)
API_PORT=3000

# WebSocket server port (realtime updates)
# Note: WebSocket is integrated into the main server, but this port is used for routing
WS_PORT=3001

# Node environment
NODE_ENV=development

# ============================================
# CORS Configuration
# ============================================
# Allowed origin for CORS (frontend URL)
# For local dev: http://127.0.0.1:8080
# For production: https://your-domain.com
CORS_ORIGIN=http://127.0.0.1:8080

# ============================================
# Google OAuth (Optional)
# ============================================
# Required if using Google OAuth authentication
# Get these from Google Cloud Console
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_CALLBACK_URL=http://localhost:3000/api/auth/google/callback

# ============================================
# MySQL Verification (Optional)
# ============================================
# Required if using Google OAuth with external MySQL verification
# Generate with: openssl rand -hex 32
MYSQL_ENCRYPTION_KEY=

# ============================================
# S3-Compatible Storage
# ============================================
# For local development with MinIO:
S3_ENDPOINT=http://localhost:9000
S3_ACCESS_KEY=minioadmin
S3_SECRET_KEY=minioadmin
S3_BUCKET_PREFIX=atlantisboard
S3_REGION=us-east-1
S3_USE_SSL=false

# For production with AWS S3:
# S3_ENDPOINT=https://s3.amazonaws.com
# S3_ACCESS_KEY=your-aws-access-key
# S3_SECRET_KEY=your-aws-secret-key
# S3_BUCKET_PREFIX=atlantisboard
# S3_REGION=us-east-1
# S3_USE_SSL=true

# ============================================
# Rate Limiting
# ============================================
# Rate limit window in milliseconds (default: 15 minutes)
RATE_LIMIT_WINDOW_MS=900000

# Maximum requests per window (default: 100)
RATE_LIMIT_MAX_REQUESTS=100

