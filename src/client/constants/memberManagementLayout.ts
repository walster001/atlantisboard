/**
 * Workspace / board member tables use `table-layout: fixed` with a dedicated role column.
 * Keep this wide enough for long custom role display names in the role {@link Select}.
 */
export const MEMBER_MANAGEMENT_ROLE_COL_PX = 260;
